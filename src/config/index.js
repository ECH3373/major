import { api } from './api';
import { app } from './app';
import { image } from './image';
import { prompt } from './prompt';
import { role } from './role';

export const config = {
  api,
  app,
  role,
  image,
  prompt
};
